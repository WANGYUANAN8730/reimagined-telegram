name: Freezing Tyrion Build
on:
  - push

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@master
      - name: spy
        run: free -m && nproc
      - name: hello
        uses: ./.github/actions/hello
